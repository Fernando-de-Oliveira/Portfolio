import { Routes } from "@angular/router";
import { UrlShorterComponent } from "./url-shorter.component";

export const URL_ROUTES: Routes = [
    {
        path: '',
        component: UrlShorterComponent,
    }
]